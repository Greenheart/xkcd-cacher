CREATE TABLE xkcd
(
    month varchar(2),
    num integer NOT NULL,
    link text,
    year varchar(4),
    news text,
    safe_title text,
    transcript text,
    alt text,
    img text,
    title text,
    day varchar(2)
);
